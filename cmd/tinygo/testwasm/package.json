{
  "name": "testjs",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tinygo build -o encrypt.wasm -target wasm -scheduler=asyncify -no-debug ..",
    "start": "npm run build && node index.js"
  }
}